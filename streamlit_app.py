import streamlit

streamlit.title('This is really interesting')
streamlit.header('The font should be smaller')
streamlit.text('And this is how the normal text should look like')
streamlit.text.bold('Will this work?')
